export const emptyObject = Object.freeze({});

export const emptyArray = Object.freeze([]);

export const cloudinaryBaseUrl = "https://res.cloudinary.com/uniformdev";

//export const siteUrl = "https://uniform.dev";
export const siteUrl = "http://localhost:3000";

export enum StaticCompositionSlugs {
  EMAIL_PREVIEW = "/emails/",
}
